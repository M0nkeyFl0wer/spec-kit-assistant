# Spec Kit Assistant - Environment Variables
# Copy this file to .env and generate secure secrets

# ============================================
# SECURITY: CRITICAL - Generate Unique Secrets
# ============================================
# DO NOT use the example values below in production!
# Generate secure secrets using: openssl rand -hex 32

# WebSocket Authentication Secret
# Used for secure WebSocket connections between swarm agents
# Generate with: openssl rand -hex 32
WEBSOCKET_AUTH_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32

# Spec Kit Secret
# Used for task signing and HMAC integrity checks
# Generate with: openssl rand -hex 32
SPEC_KIT_SECRET=CHANGE_ME_generate_with_openssl_rand_hex_32

# ============================================
# AI Model Configuration (Optional)
# ============================================

# Gemini API Key (if using Gemini coordinator)
# Get from: https://makersuite.google.com/app/apikey
# GEMINI_API_KEY=your_api_key_here

# OpenAI API Key (if using OpenAI models)
# OPENAI_API_KEY=your_api_key_here

# ============================================
# Swarm Configuration (Optional)
# ============================================

# Maximum concurrent agents
# MAX_CONCURRENT_AGENTS=10

# Swarm timeout in milliseconds
# SWARM_TIMEOUT=300000

# Enable debug logging
# DEBUG=true

# ============================================
# Remote Deployment (Optional)
# ============================================

# SSH configuration for remote swarms (Seshat, etc.)
# REMOTE_HOST=seshat.noosworx.com
# REMOTE_PORT=8888
# REMOTE_USER=m0nkey-fl0wer

# ============================================
# QUICK SETUP INSTRUCTIONS
# ============================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Generate secrets:
#    echo "WEBSOCKET_AUTH_SECRET=$(openssl rand -hex 32)" >> .env
#    echo "SPEC_KIT_SECRET=$(openssl rand -hex 32)" >> .env
#
# 3. Add API keys if needed:
#    echo "GEMINI_API_KEY=your_key_here" >> .env
#
# 4. Verify .env is in .gitignore (it should be!)
#    grep -q "^\.env$" .gitignore || echo ".env" >> .gitignore
#
# NEVER commit the .env file to git!
